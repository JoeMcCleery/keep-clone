{
  "title": "label",
  "version": 3,
  "description": "Describes a label",
  "primaryKey": "id",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "maxLength": 32,
      "format": "uuid"
    },
    "name": {
      "type": "string",
      "maxLength": 100
    }
  },
  "additionalProperties": false,
  "required": ["id", "name"],
  "indexes": ["name"]
}
